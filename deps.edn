;; No dependencies. IEC 60870-5-104 is TCP-carried integer framing (no
;; checksum — TCP already provides one) plus a handful of TLV information
;; objects. A leaf that dragged a socket or a point database behind it
;; would defeat the point, which is proving this workspace can hold a real
;; SCADA codec instead of a CRUD facade wearing the protocol's name.
{:paths ["src"]
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :cljs {:extra-paths ["test"]}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}
